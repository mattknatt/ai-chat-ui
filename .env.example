# Dev proxy target — where the Vite dev server forwards /api requests
VITE_API_TARGET=http://localhost:8080

# Set this to the deployed backend URL to point local dev at Fly
# VITE_API_TARGET=https://spring-llm-gateway.fly.dev
